import RoundedContainer from '@Components/common/RoundedContainer';

function TasksTable() {
  return (
    <RoundedContainer className="h-full w-full">task table</RoundedContainer>
  );
}

export default TasksTable;
